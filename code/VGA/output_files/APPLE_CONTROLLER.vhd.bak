library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;
use work.display_types.all;

entity PLAYER_CONTROLLER IS
	port (
		player1 : out coordinate_array(0 to MAX_ELEMENTS - 1, 0 to 1);
		player2 : out coordinate_array(0 to MAX_ELEMENTS - 1, 0 to 1)
	);
end entity;